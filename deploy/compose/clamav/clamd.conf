# ClamAV daemon config for SMAP.
# Overrides defaults that are too restrictive for SMAP file sizes.

TCPSocket 3310
TCPAddr 0.0.0.0

# SMAP TUS uploads can be up to 1 GiB; the default 25 MB is too low.
StreamMaxLength 1024M
